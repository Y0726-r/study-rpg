# 🚀 GitHubへのアップロード手順

## 📋 前提条件

- Gitがインストールされていること
- GitHubアカウントを持っていること

## 🔧 セットアップ手順

### 1. GitHubでリポジトリを作成

1. [GitHub](https://github.com)にログイン
2. 右上の「+」→「New repository」をクリック
3. リポジトリ名：`study-rpg`（任意の名前でOK）
4. Description：「勉強時間を記録するレトロRPG風アプリ」
5. Public（公開）を選択
6. 「Create repository」をクリック

### 2. ローカルでGitを初期化

```bash
# プロジェクトフォルダに移動
cd study-rpg

# Gitを初期化
git init

# README.mdのプレースホルダーを更新
# エディタで以下を置換してください：
# - "あなたのGitHubユーザー名" → 実際のGitHubユーザー名
# - "あなたの名前" → 実際の名前
# - "@あなたのTwitter" → TwitterアカウントまたはSNS（任意）

# すべてのファイルをステージング
git add .

# 初回コミット
git commit -m "Initial commit: Study RPG v1.0.0"

# メインブランチ名を設定
git branch -M main

# リモートリポジトリを追加
# ※以下のURLを自分のリポジトリURLに変更
git remote add origin https://github.com/あなたのユーザー名/study-rpg.git

# GitHubにプッシュ
git push -u origin main
```

### 3. GitHub Pagesで公開

1. GitHubのリポジトリページを開く
2. 「Settings」タブをクリック
3. 左サイドバーから「Pages」を選択
4. 「Source」で「Deploy from a branch」を選択
5. 「Branch」で「main」と「/ (root)」を選択
6. 「Save」をクリック
7. 数分待つと、URLが表示されます：
   ```
   https://あなたのユーザー名.github.io/study-rpg/
   ```

### 4. README.mdのURLを更新

公開されたら、`README.md`内のデモURLを更新してください：

```markdown
[こちらからプレイ](https://あなたのユーザー名.github.io/study-rpg/)
```

更新後、再度コミット＆プッシュ：

```bash
git add README.md
git commit -m "Update demo URL in README"
git push
```

## 🎉 完了！

これで、誰でもあなたのアプリにアクセスできます！

---

## 🔄 更新方法

コードを変更した後：

```bash
# 変更をステージング
git add .

# コミット
git commit -m "説明メッセージ"

# プッシュ
git push
```

GitHub Pagesは自動的に更新されます（数分かかる場合があります）。

---

## 📝 トラブルシューティング

### 画像が表示されない
- `style-retro.css`の画像URLが有効か確認
- または、画像を`assets/`フォルダに保存して、相対パスに変更

### GitHub Pagesが更新されない
- 数分待ってからリロード
- Settings > Pagesで再度保存してみる
- ブラウザのキャッシュをクリア

### Gitのエラーが出る
```bash
# 認証が必要な場合
git config --global user.name "あなたの名前"
git config --global user.email "your.email@example.com"

# Personal Access Tokenを使用（推奨）
# GitHub Settings > Developer settings > Personal access tokens
```

---

## 💡 ヒント

### リポジトリをプライベートにする
1. Settings > General
2. Danger Zone > Change repository visibility
3. 「Make private」を選択

### カスタムドメインを使う
1. ドメインを取得
2. Settings > Pages > Custom domain
3. CNAMEレコードを設定

### 他の人とコラボする
1. Settings > Manage access
2. 「Invite a collaborator」で招待
